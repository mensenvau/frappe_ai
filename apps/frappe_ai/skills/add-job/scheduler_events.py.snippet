# Wire scheduled jobs in frappe_ai/hooks.py, between the markers:
#
#   # >>> FRAPPEAI:scheduler_events:start
#   scheduler_events = {
#       "daily": [
#           "frappe_ai.<module>.tasks.send_daily_digest",
#       ],
#       "hourly": [
#           "frappe_ai.<module>.tasks.sync_external",
#       ],
#       "cron": {
#           "*/15 * * * *": ["frappe_ai.<module>.tasks.poll_queue"],  # every 15 min
#           "0 2 * * 1":   ["frappe_ai.<module>.tasks.weekly_cleanup"], # Mon 02:00
#       },
#   }
#   # <<< FRAPPEAI:scheduler_events:end
#
# Frequencies: all, hourly, daily, weekly, monthly, hourly_long, daily_long, ...
# The scheduler must be enabled:  bench --site <site> enable-scheduler
